{
  "$comment": "Fully local mount plan - all modules loaded from workspace directories",
  "session": {
    "orchestrator": "loop-basic",
    "context": "context-simple"
  },
  "providers": [
    {
      "module": "provider-mock",
      "config": {
        "response_mode": "echo"
      }
    }
  ],
  "tools": [
    {
      "module": "tool-echo",
      "config": {
        "prefix": "[Local] "
      }
    }
  ],
  "hooks": []
}
